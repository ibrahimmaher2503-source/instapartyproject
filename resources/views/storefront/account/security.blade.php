@extends('storefront.account.layout')
@section('account-title', __('account.security'))
@section('account-content')<form class="sf-account-panel sf-account-form" method="POST" action="{{ route('storefront.account.security.update') }}">@csrf @method('PUT')<label>{{ __('account.current_password') }}<input class="sf-field" type="password" name="current_password" autocomplete="current-password" required></label><label>{{ __('account.new_password') }}<input class="sf-field" type="password" name="new_password" autocomplete="new-password" required></label><label>{{ __('account.confirm_password') }}<input class="sf-field" type="password" name="new_password_confirmation" autocomplete="new-password" required></label><button class="sf-button sf-button--primary" type="submit">{{ __('account.change_password') }}</button></form>@endsection
